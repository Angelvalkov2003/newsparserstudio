# MongoDB. Replace <db_password> with the password for angelvalkov03_db_user.
MONGODB_URI=mongodb+srv://angelvalkov03_db_user:<db_password>@universalmarkdownbuilde.w2avogu.mongodb.net/?appName=UniversalMarkdownBuilderStudioCluster

# JWT for login tokens (set a random string in production)
JWT_SECRET=change-me-in-production
